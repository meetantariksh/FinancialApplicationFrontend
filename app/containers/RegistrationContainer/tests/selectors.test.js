// import { fromJS } from 'immutable';
// import { selectRegistrationContainerDomain } from '../selectors';

describe('selectRegistrationContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
